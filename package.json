{
  "name": "nunki",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-data": "python3 scripts/fetch_vancouver.py && python3 scripts/fetch_toronto.py && python3 scripts/fetch_benefits.py",
    "start-dev": "npm run fetch-data && npm run build && npm run dev",
    "start": "npm run fetch-data && npm run build"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
